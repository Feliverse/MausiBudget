  <header class="d-flex justify-content-between align-items-center p-2">
    <%= link_to root_path, class: 'button has-text-white' do %>
      <h4 class="material-icons-outlined">⬱</h4>
    <% end %>
 
    <div class="">
      <h5 class="title p-2 ">TRANSACTIONS</h5>
    </div>
    <%= button_to destroy_user_session_url, method: :delete, class: 'btn btn-primary p-2"' do %>
      <span >Logout</span>
    <% end %>
  </header>

  <%= render @group %>

<div>
  <P class='text-center '>DETAILS<P>
<ul class="img-thumbnail ">
  <% @group.entities.order(created_at: :desc).each do |entity| %>
    <li class="d-flex justify-content-between px-4">
      <p><%= entity.name %> </p>
      <small class="fw-bold"><%= entity.amount %></small>
    </li>
  <% end %>
</ul> 
</div>

<footer class="fixed-bottom bg-success p-3">
  <%= link_to 'Add a new transaction', new_group_entity_path(@group), class: "container btn btn-success rounded-0 btn-outline-light p-3" %>
</footer>
