<%= bootstrap_form_with(model: group) do |form| %>
  <header class="d-flex justify-content-between align-items-center">
    <h2 class="p-2" onclick="history.back()"> ⬱ </h2>
    <h5 class="p-2">NEW CATEGORY</h5>
    <small class="actions p-2">
      <%= form.submit "Next", class: "btn btn-primary" %>
    </small>
  </header>
    
  <div class='col-11 col-sm-8 col-md-10 mx-auto pt-5' >
    <div>
      <%= form.text_field :name, label_as_placeholder: true, class: 'py-3' %>
    </div>
    <div>
      <%= form.text_field :icon, label_as_placeholder: true, class: 'py-3' %>
    </div>
  </div>
<% end %>
